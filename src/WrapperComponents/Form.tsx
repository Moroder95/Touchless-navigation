import * as React from 'react';

export interface FormProps {
    
}
 
const Form: React.SFC<FormProps> = () => {
    return ( 
        <form>
            
        </form>
    );
}
 
export default Form;